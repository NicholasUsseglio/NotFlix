package com.notflix.springVideo.dto;

public interface GenericDTO { //data tranfer object per evitare di fare troppe richieste al database, o meglio per evitare di far estrapolare dati sensibili come password e username
    
}
